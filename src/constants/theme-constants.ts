import { AppTheme } from "../types";

export const APP_THEME: Record<string, AppTheme> = {
  LIGHT: "light",
  DARK: "dark",
  DEFAULT: "default",
};
export const LOCAL_STORAGE_KEY: string = "react-web-theme-value";
